spring:
  data:
    mongodb:
      uri: mongodb+srv://katarideepak_db_user:VRCpUhl5KiYvbWxD@cluster0.5jfwqxr.mongodb.net/DockerIQ?retryWrites=true&w=majority&appName=Cluster0&ssl=false
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  
  # File upload configuration for development
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB
      file-size-threshold: 2KB
      location: ${java.io.tmpdir}

logging:
  level:
    com.dockeriq.service: DEBUG
    org.springframework.data.mongodb: DEBUG
    org.springframework.web: DEBUG

server:
  port: 8080
